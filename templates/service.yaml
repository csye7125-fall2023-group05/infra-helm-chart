apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-helm-chart.fullname" . }}
  labels:
    {{- include "infra-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-helm-chart.selectorLabels" . | nindent 4 }}
